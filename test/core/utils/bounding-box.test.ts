describe('Calculate the bounding box of Gemini views correctly', () => {
    it('Linear layout, Entire bounding box', () => {
        // const t: Track = {
        //     data: { url: 'dummy', type: 'csv' },
        //     mark: 'dummy',
        //     width: 100,
        //     height: 300
        // };
        // const singleTrack = getTrackArrangementInfo({ tracks: [t] }) as BoundingBox;
        // expect(singleTrack.width).toEqual(100);
        // expect(singleTrack.height).toEqual(300);
        // const verTrack = getTrackArrangementInfo({ tracks: [t, t] }) as BoundingBox;
        // expect(verTrack.width).toEqual(100);
        // expect(verTrack.height).toEqual(600);
        // const horTrack = getTrackArrangementInfo({
        //     layout: { type: 'linear', direction: 'horizontal' },
        //     tracks: [t, t]
        // }) as BoundingBox;
        // expect(horTrack.width).toEqual(200);
        // expect(horTrack.height).toEqual(300);
        // const horWrapTrack = getTrackArrangementInfo({
        //     layout: { type: 'linear', direction: 'horizontal', wrap: 1 },
        //     tracks: [t, t]
        // }) as BoundingBox;
        // expect(horWrapTrack.width).toEqual(100);
        // expect(horWrapTrack.height).toEqual(600);
    });
});
